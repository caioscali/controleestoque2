function load_modal(quantidade, dataCadastro, idDepartamento, id, idProduto) {
  debugger;
  $('#text_id').val(id);
  $('#text_quantidade').val(quantidade);
  $('#text_dataCadastro').val(dataCadastro);
  $('#text_departamentoSelecionado').val(idDepartamento);
  $('#text_produtoSelecionado').val(idProduto);
}

