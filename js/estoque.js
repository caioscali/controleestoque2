function load_modal(quantidade, dataCadastro, idDepartamento, id, idDepartamentoProduto) {
  debugger;
  $('#text_id').val(idDepartamentoProduto);
  $('#text_quantidade').val(quantidade);
  $('#text_dataCadastro').val(dataCadastro);
  $('#text_departamentoSelecionado').val(idDepartamento);
  $('#text_produtoSelecionado').val(id);
}

