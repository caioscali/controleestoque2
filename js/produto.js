function load_modal(descricao, marca, numeroPatrimonio, idCategoria, quantidade, dataCadastro, idDepartamento, id, idDepartamentoProduto) {
  debugger;
  $('#text_id').val(id);
  $('#text_idDepartamentoProduto').val(idDepartamentoProduto);
  $('#text_descricao').val(descricao);
  $('#text_marca').val(marca);
  $('#text_numeroPatrimonio').val(numeroPatrimonio);
  $('#text_categoriaSelecionada').val(idCategoria);
  $('#text_quantidade').val(quantidade);
  $('#text_dataCadastro').val(dataCadastro);
  $('#text_departamentoSelecionado').val(idDepartamento);
}

